# 版本管理说明

不需要懂编程。看完这一页就够日常使用了。

---

## 一、这套东西解决什么问题

你让 AI 助手帮你改 Word / Excel 文档。这件事有两个天然的坑：

**坑一：你不知道助手到底改了哪里。**
它说"改好了"，可文档几十页、表格上千行，它是不是只改了该改的地方？
有没有顺手动了别的？你没法逐页核对，只能选择相信。

**坑二：你和助手会互相覆盖，而且没有任何提示。**
助手在磁盘上改完了，你这边 Word 还开着几分钟前的版本——
你一按保存，它刚才的活全没了。反过来也一样。
**不会报错，不会提示**，等发现时可能已经过去好几天。

装完这套东西：

- **助手每改一处都留痕**，你能逐段、逐单元格看清它改了什么，看完再决定认不认
- **任何时候都能翻回以前的版本**，改错了不怕
- 你自己手改的内容，助手也能看见，不会拿着旧版覆盖你

---

## 二、日常只用四条命令

先说明：**这些都是标准 git 命令**，不是我们自创的。网上的 git 教程都适用。

命令在文件夹里的终端运行。Windows 上可在文件夹地址栏输入 `cmd` 回车打开。

### 1. 看有没有人动过文件

```bash
git status --short
```

输出示例：
```
 M 报告.docx
```

- `M` = 这个文件被改过，还没存档
- `??` = 新文件，还没被管起来
- **什么都不显示** = 干净，和上次存档时一模一样

**每天开工先跑这条。** 显示空白就说明没人动过，可以放心接着干。

### 2. 看具体改了什么 ← 最常用

```bash
git diff
```

输出示例：
```
-本季度完成投资 1200 万元
+本季度完成投资 1350 万元
```

- 减号开头 = 改之前
- 加号开头 = 改之后

表格文件会显示到单元格：
```
-  B5=1200
+  B5=1350
```

**这是审核助手工作的主要手段。** 它说改了三处，你跑一下就知道是不是真的只改了三处。

只看某一个文件：
```bash
git diff -- "报告.docx"
```

### 3. 存档

```bash
git add -A
git commit -m "把第二季度投资数从1200改成1350，依据是月度进度表"
```

引号里写清楚**改了什么、依据是什么**。这句话以后就是工作记录。

助手改完通常会自己存档并告诉你。**你自己手改完也要存一次**，
否则你的改动和助手的改动混在一起，分不清也退不回去。

### 4. 看历史

```bash
git log --oneline
```

输出示例：
```
a3f9c21 补充第三部分分项说明
7b2e105 修正两处数字错误
e40d8b8 基线版本
```

前面那串字符是每次存档的编号，用来指定"回到哪一版"。

---

## 三、出事了怎么办

### 想看看以前是什么样

**安全做法**（不动现在的文件，把旧版另存一份）：
```bash
git show e40d8b8:"报告.docx" > 旧版本.docx
```
把 `e40d8b8` 换成 `git log --oneline` 里看到的编号，然后打开对照着看。

### 确定要退回上一版

```bash
git checkout HEAD~1 -- "报告.docx"
```

**这会覆盖当前文件。** 动手前先确认现在的版本不要了，
或者先用上面的安全做法另存一份。

### 内容被覆盖了

只要之前存过档就一定能找回来。跑 `git log --oneline` 找到覆盖前的那次存档，
用上面的安全做法另存出来即可。**这就是坚持存档的意义。**

---

## 四、和 AI 助手配合的规矩

### 你只需要做一件事：让助手改之前，把文档关掉

**为什么**：git 只能看见**已经保存到磁盘**的内容。
你在 Word 里改了还没保存的东西，git 和助手都看不见——
助手会以为文件还是旧的，照旧版去改，然后你一保存就把它的活覆盖了。

判断文档有没有开着：看文件夹里有没有 `~$` 开头的文件。
有的话说明有人开着（也可能是上次没正常关闭留下的残留）。

### 助手那边会遵守的规矩

- 改任何文档前，先看清你改了什么，并告诉你
- 文档还开着时不硬写，会先提醒你关闭
- 每改一处立刻存档，写明改了什么、依据是什么
- 不拿几分钟前的旧副本当"文件现在的样子"

**所以你随时可以用 `git diff` 和 `git log` 检查它有没有照做。**
这套东西的意义不只是防事故，也是让助手的工作变得**可核查**。

---

## 五、常见疑问

**问：这些命令是这个项目专用的吗？**
答：不是，全是标准 git。换个项目、换台电脑照样用。
这里只是额外配置了"让 git 看懂 Word/Excel 内容"，命令本身没有任何改动。

**问：git 会不会把我的文档改坏？**
答：不会。仓库里存的是完整的原始文件，一个字节不差。
`git diff` 显示的文字只是转换出来给你看的，不影响存储。

**问：能不能像代码那样自动合并两个人的修改？**
答：**不能。** Word/Excel 是二进制文件，git 合并不了。
所以"同一时刻只有一方在改"这条在这里比在代码项目里更重要——
代码冲突了还能合，文档冲突了只能二选一，另一边的活就白干了。

**问：`~$` 开头的文件要不要管？**
答：不用，已经设置成忽略了，不会进版本库。
确认没人开着文档时，这类残留文件可以直接删。

**问：多人协作也能用吗？**
答：可以，规矩一样——同一时刻只能一个人改，改完存档。
只是这套工具最初是为"人 + AI 助手"设计的，
因为助手改得快、改得多，更需要留痕和可核查。
